user-agent-metric:
  enabled: true
  tags:
    - OperatingSystemNameVersionMajor
    - DeviceClass
    - AgentName
    - AgentNameVersionMajor

  url-patterns: /test
  # alternatively:
  #    - /test
  #    - /test2
management:
  endpoints:
    enabled-by-default: true
    web:
      exposure:
        include: "*"
