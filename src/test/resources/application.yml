user-agent-metric:
  keys:
    - '#this.name'
    - '#this.name + "." + #this.versionNumber.major'
    - '#this.operatingSystem.name'
    - '@currentRequest.remoteAddr'
  url-patterns: /test
# alternatively:
#    - /test
#    - /test2
